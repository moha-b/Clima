import 'dart:ui';

class Weather {
  String temperature;
  String date;
  String image;
  Color textColor;
  String weatherState;
  String cityName;

  Weather(this.temperature, this.date, this.image, this.textColor,
      this.weatherState, this.cityName);
}
